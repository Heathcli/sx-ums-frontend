// 筛选项列表
export interface IFilterList {
    code:number,
    name:string|number
}

export interface UserInfo {
    studentId:number,
    name:string,
    role:string,
    roleId:number,
    position:string,
    positionId:number
}